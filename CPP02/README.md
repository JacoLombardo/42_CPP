# CPP02

Third C++ module. Fixed-point arithmetic, orthodox canonical form, operator overloading.

## Overview

- **ex00**: Fixed — basic fixed-point class
- **ex01**: Fixed — const correctness, operator overloads
- **ex02**: Fixed — comparison and arithmetic operators
- **ex03**: Point, bsp — Point class and BSP (point-in-triangle)

## Setup

```bash
cd ex00 && make && ./fixed
cd ex03 && make && ./bsp
```
